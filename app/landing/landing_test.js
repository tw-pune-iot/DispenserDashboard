'use strict';

describe('dispenser.landing module', function() {

  beforeEach(module('dispenser.landing'));

  describe('landing controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('LandingController');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});